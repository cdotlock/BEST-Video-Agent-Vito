# 短期目标

> 唯一规划文件。所有条目完成前不得开发新功能。
> 完成后删除该条目并提交。新需求追加到末尾。

- [x] Phase A: 自进化多路径视频能力（先文档后实现）
  - [x] A1. PRD 增补：自进化路径引擎、分镜拼图（4/9宫格）、首帧/首尾帧生视频、图/视频混合参考、多段视频拼接
  - [x] A2. MCP/Skills 原子化实现：新增路径规划/评审、分镜拼图、多参考生图/生视频、拼接方案工具
  - [x] A3. 记忆与流程进化：路径推荐 + 路径评审写回长期记忆
  - [x] A4. API playbook/useCase 同步

- [x] Phase B: UIUX 全量重塑（先文档后实现）
  - [x] B1. UIUX 文档重写为可落地规格（信息架构、状态、文案、交互细节、移动端、验收标准）
  - [x] B2. `/video` 与 `/video/[projectId]` 全量改版为白色高可用工作台
  - [x] B3. 新手引导（首次上手 4 步）与空态/错误引导完善
  - [x] B4. 最简单剪辑功能（多视频片段排序、时长/转场、保存拼接方案）
  - [x] B5. README 更新：详述本版本能力与使用流程

- [x] Phase C: 移除 Langfuse 依赖并内置化 Prompt 优化
  - [x] C1. 清理 Langfuse MCP/provider/skills 的默认加载与文案耦合
  - [x] C2. 用内置 `video_memory` 提供 prompt 优化与记录能力（不依赖外部 prompt 平台）
  - [x] C3. 同步更新 `.env.example` 与 `docs/api-playbook.md` / `docs/PRD.md`
  - [x] C4. 一次性拉取 Langfuse 现有 prompt 快照，沉淀到 docs 基线供后续内置 prompt 改造

- [x] Phase D: UI 质感与可读性强化（Ant Design 体系化重构）
  - [x] D1. 统一视频工作台主题 token（明确白底高对比：禁止白底白字）
  - [x] D2. 统一 `EpisodeList/ResourcePanel/VideoChat` 的字号、间距、颜色层级（去粗糙）
  - [x] D3. 新手引导改为 Ant Design `Tour + Steps` 双模式
  - [x] D4. UIUX.md 补充“视觉与交互细则 + 开源整合说明（Ant Design）”

- [x] Phase E: 对话优先与无序列阻断修复
  - [x] E1. 修复 `对话/时间线/分镜/剪辑` 在无序列时无法感知切换的问题
  - [x] E2. 新增自动工作序列初始化：首条对话可自动创建并选中默认序列（无需上传）
  - [x] E3. 同步新手引导与空态文案为“一句话启动”，保留手动上传作为可选入口

- [x] Phase F: Chat 主控与默认风格策略重构
  - [x] F1. 默认内置风格库（数十个）自动匹配，首轮不强制风格自定义
  - [x] F2. 仅在用户明确要求或对风格不满意时触发风格定制流程
  - [x] F3. 中央工作台去步骤卡，改为 Chat 主舞台，术语从“序列”收敛为“计划”
  - [x] F4. 剪辑视图重构为“预览 + 时间轨 + 片段属性”的剪辑台形态

- [x] Phase G: 结构再重构（左栏计划中枢 + 时间线条状 + 可视化剪辑）
  - [x] G1. 左栏去导航，重构为 `风格/计划/历史 + Agent 实时计划`
  - [x] G2. 取消时间线独立看板，改为素材区按钮打开条状时间线
  - [x] G3. 取消分镜看板，素材改为可展开面板并承载 prompt/继续生成/风格参考动作
  - [x] G4. 剪辑改为可视化粗剪（预览+轨道），自动加载/自动生成粗剪，不再让用户理解 plan 结构
  - [x] G5. 执行模式规则：checkpoint 先出计划并确认后执行；yolo 先出计划后自动执行；计划常驻左栏

- [x] Phase H: 对话主控精修（@素材、首屏重构、可视化剪辑强化）
  - [x] H1. 文档先行：UIUX.md 增补 v1.4（首屏大 Chat、左栏中枢、素材详情弹窗、提示词保护）
  - [x] H2. 素材交互重做：`@该素材` 注入上下文、`设为新风格` 同级按钮、缩略图点开详情（左图右 prompt + 可编辑 + 重 roll）
  - [x] H3. 提示词保护：不向终端玩家暴露系统内置 prompt，仅暴露可编辑创作描述层
  - [x] H4. 首屏重构：`/video` 改为中间大聊天入口，下方展示“已创建故事”
  - [x] H5. 左栏重构：风格 + 计划 + 历史合计站，计划简化为 markdown 展示并支持 checkpoint 确认
  - [x] H6. Pro 模式：后置到二级入口，避免主界面概念拥挤（按需再开）
  - [x] H7. 剪辑增强：可视化拖动调整 in/out、粗剪自动加载并可预览
  - [x] H8. 稳定性修复：补强 key_resource 与 stream_end 刷新链路，降低素材未入库的可见性回归

- [x] Phase I: 左栏语义收敛 + Pro 开口 + 剪辑引擎升级（OpenCut/OpenReel 参考）
  - [x] I1. 左栏收敛：底部由“计划列表”改为“会话历史”，计划仅保留顶部卡片展示
  - [x] I2. 计划卡升级：支持折叠/展开，并提供简单可视化（步骤进度、执行状态）
  - [x] I3. Pro 模式入口：顶部与左栏均可进入，开放工作流偏好、memory user、风格定制高级项
  - [x] I4. 剪辑引擎方案文档：基于 OpenCut / openreel-video 的能力对齐与接入边界（组件复用优先，避免重写）
  - [x] I5. 剪辑台能力升级：拖拽重排、实时粗剪串播预览、拖动裁切、自动粗剪计划执行与预览闭环
  - [x] I6. 技术验证：选定一条开源接入路径并完成 PoC（最小可用时间线 + 预览联动）
  - [x] I7. 提交与同步：完成功能后执行 lint/tsc、提交并推送到 `origin/main`
